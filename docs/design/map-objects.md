# 맵 오브젝트 18종 — 실측과 역할

> 2026-08-05 작성. 아트는 8/8 이후 교체 예정이며 그때 이 표의 오프셋 열이 사라진다.
> 배치·파괴 작업 항목은 [`docs/TODO.md`](../TODO.md) 를 본다.

---

## 1. 토템 — 파괴가 층의 진행을 만든다

| 역할 | 파괴하면 | 어느 층에 |
|---|---|---|
| **메인 토템** | **그 자리에 포탈이 생긴다.** 다음 층으로 가는 유일한 길 | 1F · 2F 에 각 1개 |
| **서브 토템** | 그 층에 출현하는 **몬스터 총량이 줄어든다** | 1F · 2F. 개수 미정 |

**3F(보스층)에는 토템을 두지 않는다** (2026-08-05 확정).
보스를 처치하면 이펙트와 함께 **그 자리에 금색 보상 포탈**이 열린다.
토템이 하던 "층 클리어 조건" 역할을 보스가 대신하므로 중복이다.

```
1F · 2F   서브 토템으로 압박 조절  →  메인 토템 파괴  →  포탈
3F        보스 처치               →  이펙트          →  금색 보상 포탈
```

**메인과 서브는 서로 다른 자산을 쓴다.** 같은 그림이었다면 플레이어가 무엇을 부숴야
포탈이 열리는지 알 수 없었을 텐데, 아트가 갈려 있어 그 문제가 없다.

| 테마 | **메인 토템** ★ | 접지폭 | **서브 토템** ☆ | 접지폭 |
|---|---|---|---|---|
| Goblin | `06_scrap_totem` | 0.80칸 | `01_twisted_stump` | 1.75칸 |
| Orc | `05_war_totem` | 0.84칸 | `01_scorched_stump` | 1.62칸 |
| Vampire | `06_bloodstone_altar` | 1.69칸 | `03_sarcophagus` | 1.69칸 |

읽히는 규칙이 있다. **메인 토템은 세로로 서 있고 밑동이 좁으며(제단만 예외),
서브 토템은 낮고 넓게 퍼져 있다.** Goblin·Orc 는 둘 다 그루터기라 계열이 같고,
Vampire 만 석관이지만 실루엣의 성격은 같다. 이 대비가 학습 없이도 역할을 알려준다.

### 왜 이 구조인가

층을 "끝까지 걸어가면 끝"이 아니라 **선택이 있는 공간**으로 만든다.

```
메인 토템만 부수고 통과   →  빠르지만 내내 몬스터에 쫓긴다
서브 토템부터 정리        →  느리지만 층이 조용해진다
```

파밍 층인 2F 에서 특히 의미가 커진다. 경험치를 벌려면 몬스터가 많아야 하고,
안전하게 이동하려면 적어야 한다. **서브 토템이 그 조절 손잡이**가 된다.

결정 005 §4 의 "보주 파괴 시 리스폰도 함께 멈춘다 — 안 그러면 포탈로 걸어가는 동안
계속 새 몬스터에게 쫓긴다" 가 예고한 구조가 이것이다. **토템이 그 보주다.**

### 인구는 지분으로 나눈다 — 메모리 할당 모델 (2026-08-05 확정)

**메인 토템이 기본 점유를 잡고, 서브 토템이 그 위에 추가 점유를 얹는다.**

```
층 인구 = 메인 토템의 기본 점유
        + Σ (살아 있는 서브 토템의 추가 점유)
```

예를 들어 기본 4 · 서브 토템 3개가 각 4씩이면 `16 → 12 → 8 → 4` 로 줄어든다.

**메인 토템을 부수면 기본 점유가 통째로 풀린다.** 할당의 밑동이 사라지므로
그 위의 지분도 함께 사라지고 층 인구가 **0** 이 된다. 리스폰도 그 시점에 멈춘다.

### 이미 나와 있는 몬스터는 사라지지 않는다 (2026-08-05 확정)

메인이든 서브든, 토템을 부숴도 **살아 있는 개체는 그대로 남는다.**
줄어드는 것은 목표 인구뿐이고, 실제 수는 처치되면서 자연히 수렴한다.

```
서브 토템 파괴  →  목표 16 → 12.  화면의 16마리는 그대로 있다
                   죽어도 12마리가 될 때까지 새로 안 채운다
메인 토템 파괴  →  목표 0.  남은 몬스터를 뚫고 포탈까지 가야 한다
```

**부수는 순간 몬스터가 증발하면 그 행동이 공짜가 된다.** 남겨두면
"지금 부술까, 정리하고 부술까" 가 실제 판단이 되고, 포탈까지 가는 길에도 긴장이 남는다.

구현상으로도 이쪽이 단순하다. `FloorPopulation` 은 이미
`alive.Count >= targetPopulation` 일 때 **채우기를 멈출 뿐 회수하지 않는다.**
목표치를 낮추는 API 만 열면 그대로 동작한다.

이건 결정 005 §4 가 요구한 동작 그대로다 —
"보주 파괴 시 리스폰도 함께 멈춘다. 안 그러면 포탈로 걸어가는 동안 계속 새 몬스터에게 쫓긴다."
**메인 토템 파괴 = 포탈 개방 = 층 정리**가 한 동작으로 묶인다.

그래서 서브 토템은 "포탈을 열기 전까지의 압박을 얼마나 낮출 것인가" 를 고르는 손잡이다.
메인을 먼저 부수면 서브는 의미를 잃는다 — **그게 의도된 선택지**다.

| 상태 | 인구 | 플레이어가 얻는 것 |
|---|---|---|
| 아무것도 안 부숨 | 16 | 최대 파밍 효율 |
| 서브 2개 파괴 | 8 | 이동이 편해진다 |
| 메인 파괴 | **0** | 포탈이 열리고 층이 조용해진다 |

전부 부순 뒤에도 파밍하고 싶다면 층을 나갔다 다시 들어와야 하는데,
파괴 상태가 저장되므로 되살아나지 않는다. **한 층의 파밍은 유한하다**는 뜻이며
이것이 다음 층으로 미는 힘이 된다.

---

## 2. 실측 — 접지선이 테마마다 다르다

알파 경계 기준. 캔버스는 전부 128 × 128, PPU 64 → **1칸 = 64px**.

| 테마 | 하단 여백 | 정렬 |
|---|---|---|
| Goblin | 8 ~ 25px (제각각) | 캔버스 중앙 |
| **Orc** | **전부 7px** | **바닥** |
| Vampire | 9 ~ 40px (제각각) | 캔버스 중앙 |

Pivot 이 Center 이므로 같은 Y 에 놓으면 **겉보기 접지점이 최대 33px(0.5칸) 어긋난다.**
어떤 것은 땅에 박히고 어떤 것은 떠 보인다.

캐릭터가 쓰는 방식(root = 접지점, `Visual` 자식에 오프셋 `0.375`)을 그대로 적용한다.

| 오브젝트 | bbox(칸) | 접지폭 | **Visual Y** | 성격 |
|---|---|---|---|---|
| **G 01 twisted_stump ☆** | 1.75 × 1.66 | 1.75 | 0.828 | **서브 토템** |
| G 02 moss_boulder | 1.61 × 1.62 | 1.52 | 0.813 | 덩어리 |
| G 03 gold_vein_rock | 1.33 × 1.28 | 1.28 | 0.641 | 덩어리 |
| G 04 mushroom_cluster | 1.34 × 1.23 | 1.25 | 0.609 | 낮게 퍼짐 |
| G 05 supply_pile | 1.56 × 1.22 | 1.48 | 0.609 | 낮게 퍼짐 |
| **G 06 scrap_totem ★** | 1.11 × 1.75 | **0.80** | 0.875 | **메인 토템** |
| **O 01 scorched_stump ☆** | 1.62 × 1.69 | 1.62 | 0.891 | **서브 토템** |
| O 02 iron_boulder | 1.62 × 1.52 | 1.47 | 0.891 | 덩어리 |
| O 03 weapon_rack | 1.64 × 1.75 | 1.59 | 0.891 | **정면 뷰 — 벽 부착용** |
| O 04 supply_pile | 1.56 × 1.33 | 1.52 | 0.891 | 낮게 퍼짐 |
| **O 05 war_totem ★** | 1.45 × 1.75 | **0.84** | 0.891 | **메인 토템** |
| O 06 fire_pit | 1.44 × 1.11 | 1.23 | 0.891 | 바닥에 깔림 |
| V 01 dead_tree | 1.58 × 1.73 | 1.27 | 0.859 | 세로 · 가지가 넓음 |
| V 02 gravestone_cluster | 1.55 × 1.53 | 1.55 | 0.766 | 세로 |
| **V 03 sarcophagus ☆** | 1.69 × 1.08 | 1.69 | 0.531 | **서브 토템** |
| V 04 candle_cluster | 0.86 × 1.20 | **0.48** | 0.594 | 가장 가늘다 |
| V 05 thorned_roses | 1.41 × **0.77** | 1.39 | 0.375 | 가장 낮다 |
| **V 06 bloodstone_altar ★** | 1.69 × 1.52 | **1.69** | 0.750 | **메인 토템** |

**콜라이더는 bbox 가 아니라 접지폭을 쓴다.** 탑다운이라 지면 충돌 영역과 상단 가림 아트를
분리해야 한다 (CLAUDE.md §4). `dead_tree` 는 가지가 1.58칸이지만 몸통은 1.27칸이고,
`candle_cluster` 는 0.48칸이라 옆으로 지나갈 수 있어야 자연스럽다.

메인 토템 3종 중 Goblin·Orc 는 밑동이 0.8칸으로 좁은데 **Vampire 제단만 1.69칸**이다.
토템 자리가 곧 포탈 자리이므로, 제단은 파괴 후 2×2 포탈이 그 자리에 정확히 들어간다.

> **8/8 아트 교체 시 규약** — 18장의 하단 여백을 **7px 로 통일**하면 위 `Visual Y` 열이
> 통째로 사라지고 전부 `0.891` 이 된다. Codex 쪽에 이 규약을 함께 준다.

---

## 3. 데이터 구조

```
PropDefinition (SO)              수치만. 씬 참조 없음
    propId · displayName · theme
    maxHp · isDestructible
    role : Decoration | Destructible | SubTotem | MainTotem
    expReward · dropTable
    populationShare                서브 토템이 담당하는 인구 지분

Prop.prefab + 18 배리언트          콜라이더 · Visual 오프셋은 프리팹에 굽는다
    └ Visual  [C] SpriteRenderer
    [C] CapsuleCollider2D          접지폭 × 0.5칸
    [C] Destructible               HP · 피격 · 파괴 이벤트

SpawnTotem                        Destructible 을 구독
    MainTotem → 같은 자리의 Portal 활성화
    SubTotem  → FloorPopulation 의 목표 인구 감소
```

콜라이더 크기와 오프셋을 SO 가 아니라 프리팹에 두는 이유는 **에디터에서 눈으로 맞춰야 하는 값**이기
때문이다. 몬스터가 `MonsterDefinition` 으로 수치만 갈아 끼우는 것과 같은 분리다.

### 토템 자리 = 포탈 자리

포탈을 런타임에 만들지 않고 **같은 자리에 미리 두고 꺼 둔다.**

```
TotemSite  (빈 GameObject — 여기가 포탈이 생길 자리)
 ├─ Totem   [Prop 배리언트 · Destructible · SpawnTotem]   고체 콜라이더
 └─ Portal  [Portal · BoxCollider2D(Trigger) 2×2]         비활성으로 시작
```

이유가 둘이다. `Portal` 은 목적지 `AreaDefinition` + arrivalId 링크를 들고 있어야 하는데
런타임 생성이면 에디터에서 지정할 데가 없다. 그리고
`Pretty Knights > Areas > 0. 포탈 링크 점검` 이 **비활성 포탈까지 훑으므로**,
미리 배치해두면 토템을 부숴보기 전에 링크 오타가 잡힌다.

---

## 4. 랜덤 배치 — 몬스터와 다르게 다뤄야 한다

`FloorPopulation`(몬스터)을 그대로 복제하면 안 되는 이유가 셋이다.

**① 오브젝트는 길을 막는다.** 몬스터는 죽이거나 밀고 지나가면 되지만 2×2 고체 콜라이더는 남는다.
최악은 **토템이나 포탈이 봉쇄되어 층을 클리어할 수 없게 되는 것**이다.

검사는 **"반드시 도달해야 하는 지점"** 만 본다 — 시작 지점 → 토템 → 포탈.

```
장애물   벽(Guide) + 모든 오브젝트(수동 배치 포함)
목표     필수 지점 전부에 도달 가능한가
위반     그 경로를 막는 오브젝트를 배치에서 뺀다
```

**히든 방은 필수 지점이 아니므로 자동으로 예외가 된다.**
오브젝트로 입구를 막아 만드는 공간이고 손으로 배치하므로, 검사가 "도달 불가" 라고
판정해도 그건 의도된 상태다. 별도 규칙이 필요 없다.

> **손으로 배치한 오브젝트는 도구가 건드리지 않는다.**
> 배치 도구는 자기가 뿌린 것만 관리하고, 수동 배치분은 **회피 대상**으로만 읽는다.
> 히든 방 입구를 스캐터가 지우거나 그 위에 겹쳐 놓으면 설계가 깨진다.

### 히든 방 내부는 `NoSpawnZone` 으로 비운다 (2026-08-05 확정)

방 **안쪽**에 잡동사니가 뿌려지는 것은 위 규칙으로 막히지 않는다. 별도 구역 표시가 필요하다.

**`WalkableArea` 에서 배제하는 방식은 쓰지 않는다.** 그쪽은 "여기 설 수 있는가" 라는
지형의 사실을 답하는 자리이고, 히든 방은 부수고 들어가면 실제로 설 수 있는 곳이다.
거기서 빼면 이런 것들이 히든 방을 "갈 수 없는 곳" 으로 읽는다.

| 깨지는 것 | 증상 |
|---|---|
| `GameRoot.RestorePlayerPosition` | 히든 방에서 저장 후 재시작 → 방 밖으로 튕기거나 복원이 무시된다 |
| `AreaTransition` 도착 보정 | 같은 이유 |
| 그리드 A\* (TODO 4) | 몬스터가 통행 불가로 읽어 플레이어를 따라 들어오지 못한다 |

막고 싶은 것은 지형이 아니라 **뿌리는 행위**이므로 층위를 따로 둔다.

```
NoSpawnZone  →  배치 도구        오브젝트를 안 뿌린다
             →  FloorPopulation  몬스터를 안 뿌린다
             →  WalkableArea     모른다 (이동·복원·경로탐색 영향 없음)
```

**몬스터 차단이 오브젝트보다 급하다.** 봉인된 방에 몬스터가 스폰되면
몬스터는 오브젝트를 부술 수 없어 영영 갇히고, 그 상태로 **층 인구 상한을 계속 차지한다.**
토템 지분 모델이 그대로 샌다. 지저분한 것보다 이쪽이 실제 버그다.

물리를 쓰지 않는 사각형 하나이며, 모양이 복잡한 방은 여러 개 겹쳐 놓는다.
콜라이더로 만들면 레이어를 맞춰야 하고 실수로 무언가와 부딪힌다.

**② `WalkableArea.IsWalkable` 은 점 하나만 본다.** 2×2칸 전부와 최소 간격까지 봐야 하므로
`IsAreaWalkable(center, size)` 가 필요하다.

**③ 배치는 다시 와도 그대로여야 한다.** ← 이것이 방식을 결정한다

> **탈출 스킬로 던전을 나갔다가 다시 들어와도 오브젝트가 그 자리에 있어야 한다.**
> 사라지거나 재배치되면 플레이어가 길을 처음부터 다시 찾아야 한다.
> 오브젝트가 통행 경로를 만드는 이상, **배치는 그 층의 지형 그 자체**다.

두 경우를 구분해야 한다.

| 상황 | 지금 구조에서 | 필요한 것 |
|---|---|---|
| **세션 안에서 탈출 → 복귀** | 층 오브젝트를 껐다 켜는 것뿐이라 **그대로 유지된다** | 없음 — 단, 진입할 때마다 재배치하면 깨진다 |
| **게임 재시작 후 복귀** | 파괴 기록이 사라져 부순 토템이 되살아난다 | **파괴 목록을 세이브에** |

| 안 | 방식 | 재진입 시 배치 | 저장할 것 | 로딩 |
|---|---|---|---|---|
| **A. 에디터 베이크** | 도구로 뿌린 뒤 씬에 굽는다 | **항상 같음** | 파괴 목록만 | 없음 |
| B. 시드 저장 | 구역별 시드를 세이브에 | 같음 | 시드 + 파괴 목록 | 층 진입마다 |
| ~~C. 매번 새로~~ | 진입 때마다 재배치 | **매번 다름** | 없음 | 층 진입마다 |

**C 는 탈락한다.** 탈출 후 복귀할 때마다 지형이 바뀌어 위 요구를 정면으로 위반한다.
환생 시 재배치도 같은 이유로 신중해야 한다 — 환생은 "새 회차" 라는 명확한 경계가 있으므로
그때만 허용한다.

**A 를 권한다.** 9층 81,373칸을 이미 손으로 배치했는데 오브젝트만 런타임 랜덤일 이유가 없다.
뿌린 뒤 **손으로 다듬을 수 있는 것**이 크다 — 토템 주변을 비우거나 보스방 중앙을 트는 식.
2만 칸 층에 수백 개를 런타임 `Instantiate` 하면 진입할 때마다 프레임이 튄다.

### 파괴 상태를 저장하려면 안정적인 ID 가 필요하다

세이브에 "부순 것" 을 적으려면 오브젝트마다 **재시작해도 같은 이름표**가 있어야 한다.
`GetInstanceID` 는 실행마다 바뀌므로 쓸 수 없다.

에디터 베이크 방식이면 배치 시점에 번호를 굽는다 — 예: `구역 areaId + 칸 좌표`.
좌표 기반이면 배치를 손으로 옮겨도 그 오브젝트의 정체가 따라 움직여 안전하다.

```
SaveData
  destroyedProps : [ { areaId, cellX, cellY } ... ]
```

층을 켤 때 이 목록과 대조해 해당 오브젝트를 꺼 두고,
메인 토템이 목록에 있으면 그 자리의 포탈을 처음부터 켠 상태로 둔다.

---

## 5. 선행 조건 — 부술 수단이 없다

**플레이어 공격이 아직 없다.** 이 문서의 거의 전부가 거기에 걸려 있다.

| 막힌 것 | 원인 |
|---|---|
| 몬스터 HP 밸런싱 | 플레이어 DPS 를 계산할 수 없다 |
| 메인 토템 파괴 → 포탈 개방 | 부술 수단이 없다 |
| 서브 토템 파괴 → 인구 감소 | 부술 수단이 없다 |
| 오브젝트 파괴 → 드랍 | 부술 수단이 없다 |
| 데미지 공식 확정 | 실제로 때려볼 수 없다 |

다섯 다 **같은 하나**에 걸려 있으므로 스킬 판정을 앞으로 당기는 것을 검토한다.

---

## 6. 미결

- **서브 토템 층당 개수**와 각자의 추가 점유량 (기본 점유량도 함께)
- `weapon_rack`(Orc 03)은 정면 뷰 — 벽 부착 전용인지
- 층당 오브젝트 밀도 (Goblin 2F 20,035칸 기준 몇 개인지)
- 히든 방을 만들 때 쓸 오브젝트 종류와 입구 규격 (몇 개를 부숴야 뚫리는지)
